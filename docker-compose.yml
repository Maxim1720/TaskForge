version: "3"
services:
  frontend:
    build: ./frontend/
    ports:
      - "5173:5173"
  pg:
    image: "postgres:15-alpine"
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=task_tracker
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_USER=postgres
